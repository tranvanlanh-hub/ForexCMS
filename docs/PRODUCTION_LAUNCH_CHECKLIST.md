# Production launch checklist — checkpoint 39

Status on 2026-09-10: **preview ready for technical/editorial testing; production NOT ready**. This checklist is preparation, not authorization to deploy.

| Gate | Evidence / action | Owner | State |
|---|---|---|---|
| Admin authentication | Apply the single-admin auth migration, generate independent auth secrets, run `admin:setup`, enroll TOTP, store recovery codes offline, and verify unauthenticated redirect plus password/TOTP/session/logout behavior. Never paste credentials, TOTP secrets, or recovery codes into docs. | Owner | Pending |
| Production target | Explicitly select intended Worker/environment. Old `content-hub-cms` still exists remotely with a D1 `DB` binding and no observed CMS secrets. Do not reuse its settings blindly. Prepare a reviewed PostgreSQL/Neon production configuration with no D1 binding before approved deployment. Preview has no D1 binding. | Owner + implementer | Blocker |
| Domain | Supply real domain, verify DNS ownership, HTTPS and intended Worker route; choose one canonical hostname and redirect aliases. | Owner | Pending |
| Runtime variables | Set APP_ENV=production and APP_URL=https://the-approved-domain (no localhost/workers.dev). Ensure runtime settings and generated deploy configuration agree. APP_ENV is required by the existing Neon adapter selection. | Implementer after approval | Pending |
| Neon production identity | Record privately the intended project/branch/database/role. Decide whether to clone preview into a separate production branch; never assume matching secrets because names match. Local Neon URL is pooled, TLS required; all 7 migration checksums match. | Owner | Pending |
| Required secrets | Current source needs DATABASE_URL, AUTH_PASSWORD_PEPPER, and AUTH_ENCRYPTION_KEY. The remote preview still has the former Basic Auth secrets until the new source is explicitly deployed; do not treat those as satisfying the new auth requirements. | Owner | Production pending |
| Media | If publishing with uploaded images, configure S3_ENDPOINT, S3_REGION, S3_BUCKET, S3_PUBLIC_BASE_URL and S3_ACCESS_KEY_ID/S3_SECRET_ACCESS_KEY; test read/write and backup. No media secrets observed on preview. Otherwise explicitly launch without upload dependency. | Owner | Decision pending |
| Initial Neon backup | Create full PostgreSQL custom-format dump using existing backup script with DATABASE_URL loaded securely; encrypted off-device copy, timestamp, size and checksum. Restore to a separate disposable database and compare row counts/migrations. `pg_dump` is not available on this machine's PATH. The ignored pilot repair JSON snapshots are NOT full backups. | Owner | Blocker |
| Repo secrets | 202 candidate working files and 104 historical file versions across 2 commits scanned: no known credentials, Neon credential URLs or private key headers. Private env files ignored; only env examples tracked. This is a bounded scan, not a guarantee against all secret formats. Scan staged diff again before commit. | Implementer | Passed within scope |
| Public content | Current 16 published rows are technical pilot content; SampleFX is fictional, review text/facts include demo material, Vietnamese drafts require proper editorial localization. Remove demo/test pages from production publication, replace test affiliate destinations, verify sourced facts and author/reviewer identity. | Editor + owner | Blocker |
| Robots / canonical / sitemap | Preview currently allows crawling and uses its own origin consistently. Before public launch, set an explicit policy to prevent duplicate indexing of preview (access protection or suitable noindex policy); production public pages must be indexable, admin private, canonical/hreflang/sitemap all on approved domain. Re-test on actual domain after deployment. | Owner + implementer | Pending |
| Smoke and monitoring | Run homepage, authenticated/unauthenticated admin, both sitemaps, EN/VI article, broker review, eligible comparison, configured test CTA and draft 404. Verify uptime, runtime error observation and Neon connection health; name an incident owner. | Implementer | Preview passed; production pending |
| Release authorization | Review commit(s), clean generated artifacts, migration status, backup evidence, final config and rollback plan. Obtain explicit production deploy confirmation from owner. | Owner | NOT authorized |

Preview settings observed through Cloudflare API: APP_ENV=preview; APP_URL=https://content-hub-cms-preview.content-hub-stack.workers.dev; secrets DATABASE_URL, ADMIN_USERNAME, ADMIN_PASSWORD; ASSETS and version metadata. No D1 or R2 bindings observed on preview. Metadata does not reveal the exact DATABASE_URL secret value.

After explicit deployment approval: use a production-specific reviewed config, apply only pending committed migrations via `prisma migrate deploy` (never `migrate dev` or reset), deploy the reviewed build, verify HTTPS/auth/SEO/CTA and inspect logs. If smoke fails, pause launch/content publishing and revert the application to the last known-good version; restore database only through a separately reviewed recovery procedure. Do not drop Neon data to solve an application failure.

Verification of this checklist: checked every user-required launch item against current repo, remote binding metadata and actual preview smoke evidence. No production mutation, secret rotation, domain change or deployment was performed.

Backup reference: [Neon migration using pg_dump/pg_restore](https://neon.com/docs/import/migrate-from-neon). Use a client version compatible with the server, keep dumps private, and validate restoration; the full backup remains pending.
